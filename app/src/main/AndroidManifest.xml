<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quick.dospbsparepart"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        tools:replace="icon, label"
        android:label="DO SPB Sparepart"
        android:allowBackup="true"
        android:icon="@mipmap/ic_app"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VerifikasiAllocateActivity" />
        <activity android:name=".ItemCollyActivity"
            android:windowSoftInputMode="stateAlwaysHidden"
            />
        <activity android:name=".BagiCollyActivity" />
        <activity android:name=".DOCollyActivity" />
        <activity android:name=".ItemAllocateActivity" />
        <activity android:name=".DOAllocateActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>